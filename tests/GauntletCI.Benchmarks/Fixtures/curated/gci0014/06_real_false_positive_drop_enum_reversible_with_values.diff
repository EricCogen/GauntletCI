# origin: real
# source: https://github.com/rails/rails/pull/45774
# expected: do-not-fire
# reason: Migration inversion is hardened so drop_enum is reversible when enum values are provided, improving rollback safety.

--- a/activerecord/lib/active_record/migration/command_recorder.rb
+++ b/activerecord/lib/active_record/migration/command_recorder.rb
@@ -285,6 +285,12 @@ def invert_remove_exclusion_constraint(args)
           super
         end
 
+        def invert_drop_enum(args)
+          _enum, values = args.dup.tap(&:extract_options!)
+          raise ActiveRecord::IrreversibleMigration, "drop_enum is only reversible if given a list of enum values." unless values
+          super
+        end
+
         def respond_to_missing?(method, _)
           super || delegate.respond_to?(method)
         end
