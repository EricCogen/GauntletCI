# origin: synthetic
# expected: fire
# reason: Writes user data to a new blob storage path without maintaining a backward-compatible read from the old path.

--- a/storage/user_store.go
+++ b/storage/user_store.go
@@ -8,11 +8,8 @@ func (s *UserStore) SaveProfile(ctx context.Context, userID string, data []byte) error {
-    // Write to both old and new paths for backward compatibility
-    oldPath := fmt.Sprintf("profiles/%s.json", userID)
-    newPath := fmt.Sprintf("v2/profiles/%s/data.json", userID)
-    if err := s.blob.Put(ctx, oldPath, data); err != nil {
-        return fmt.Errorf("old path write: %w", err)
-    }
-    return s.blob.Put(ctx, newPath, data)
+    newPath := fmt.Sprintf("v2/profiles/%s/data.json", userID)
+    return s.blob.Put(ctx, newPath, data)
 }
 
 func (s *UserStore) LoadProfile(ctx context.Context, userID string) ([]byte, error) {
-    oldPath := fmt.Sprintf("profiles/%s.json", userID)
-    newPath := fmt.Sprintf("v2/profiles/%s/data.json", userID)
-    data, err := s.blob.Get(ctx, newPath)
-    if err != nil {
-        // fall back to old path for users not yet migrated
-        return s.blob.Get(ctx, oldPath)
-    }
-    return data, nil
+    return s.blob.Get(ctx, fmt.Sprintf("v2/profiles/%s/data.json", userID))
 }
