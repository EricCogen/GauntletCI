# origin: synthetic
# expected: fire
# reason: Migration drops the 'legacy_code' column from the orders table — this is irreversible once deployed to production.

--- a/migrations/0045_drop_legacy_code.go
+++ b/migrations/0045_drop_legacy_code.go
@@ -0,0 +1,18 @@
+package migrations
+
+import "database/sql"
+
+// Migration0045 drops the legacy_code column from orders.
+type Migration0045 struct{}
+
+func (m Migration0045) Up(db *sql.DB) error {
+    _, err := db.Exec(`ALTER TABLE orders DROP COLUMN legacy_code`)
+    return err
+}
+
+func (m Migration0045) Down(db *sql.DB) error {
+    // no rollback — column data is permanently lost
+    return nil
+}
