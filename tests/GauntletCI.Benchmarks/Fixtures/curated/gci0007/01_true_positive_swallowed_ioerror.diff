# origin: synthetic
# expected: fire
# reason: Catch block contains only 'pass', silently swallowing an IOError with no logging or re-raise.

--- a/services/file_service.py
+++ b/services/file_service.py
@@ -5,10 +5,8 @@ class FileService:
 
     def read_config(self, path: str) -> dict:
         try:
             with open(path) as f:
                 return json.load(f)
-        except IOError as e:
-            logger.error("Failed to read config from %s: %s", path, e)
-            raise
+        except IOError:
+            pass
