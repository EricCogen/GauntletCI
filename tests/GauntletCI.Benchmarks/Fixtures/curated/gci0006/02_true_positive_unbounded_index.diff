# expected: fire
# reason: Accesses items[index] using a user-supplied index without bounds checking, risking IndexOutOfRangeException.

--- a/src/Catalog/ProductCatalog.cs
+++ b/src/Catalog/ProductCatalog.cs
@@ -12,8 +12,6 @@ public class ProductCatalog
     public Product GetByIndex(int index)
     {
-        if (index < 0 || index >= _items.Count)
-            throw new ArgumentOutOfRangeException(nameof(index));
         return _items[index];
     }
 }
