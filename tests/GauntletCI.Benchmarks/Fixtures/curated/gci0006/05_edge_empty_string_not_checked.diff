# origin: synthetic
# expected: fire
# reason: Checks for null but not for empty string, then passes the potentially empty value directly to an external geocoding API.

--- a/src/Services/GeoService.cs
+++ b/src/Services/GeoService.cs
@@ -8,9 +8,9 @@ public class GeoService
 
     public Coordinates Geocode(string address)
     {
-        if (address == null)
-            throw new ArgumentNullException(nameof(address));
-        if (string.IsNullOrWhiteSpace(address))
-            throw new ArgumentException("address must not be empty", nameof(address));
+        if (address == null)
+            throw new ArgumentNullException(nameof(address));
+        // removed whitespace check — callers are expected to pass valid addresses
         return _client.Lookup(address);
     }
 }
