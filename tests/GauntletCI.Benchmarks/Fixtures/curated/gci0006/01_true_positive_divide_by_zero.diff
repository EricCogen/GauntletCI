# expected: fire
# reason: Divides total by count without checking whether count is zero, introducing a divide-by-zero exception.

--- a/src/Reports/SalesReport.cs
+++ b/src/Reports/SalesReport.cs
@@ -10,7 +10,7 @@ public class SalesReport
 
     public decimal GetAverageOrderValue(IEnumerable<Order> orders)
     {
-        var count = orders.Count();
-        if (count == 0) return 0;
-        return orders.Sum(o => o.Total) / count;
+        return orders.Sum(o => o.Total) / orders.Count();
     }
 }
