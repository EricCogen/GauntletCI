# origin: synthetic
# expected: do-not-fire
# reason: Adds an explicit null guard at method entry before using the parameter — the edge case is properly handled.

--- a/src/Services/NotificationService.cs
+++ b/src/Services/NotificationService.cs
@@ -8,6 +8,9 @@ public class NotificationService
 
     public void Send(string recipient, string message)
     {
+        if (recipient == null) throw new ArgumentNullException(nameof(recipient));
+        if (message == null) throw new ArgumentNullException(nameof(message));
+
         _emailClient.Send(recipient, message);
     }
 }
