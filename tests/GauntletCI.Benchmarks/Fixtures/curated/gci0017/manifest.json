{
  "mapped_gci_rules": [
    "GCI0003",
    "GCI0004",
    "GCI0016"
  ],
  "description": "Sync-over-async + resource leak: removes IDisposable, introduces Task.Run().Result blocking in UI property getter.",
  "fixtures": [
    {
      "id": "gci0017-11",
      "diff_file": "11_real_true_positive_resource_leak_plus_deadlock.diff",
      "category": "true-positive",
      "expected_outcome": "fire",
      "expected_gci_rules": [
        "GCI0003",
        "GCI0004",
        "GCI0016"
      ],
      "notes": "Removes IDisposable implementation (resource leak on every page render), introduces Task.Run().Result blocking in UI property getter, and drops the null-check guard — memory leak plus deadlock risk with no tests.",
      "origin": "real",
      "source_url": "https://github.com/CoraCodeDev/PdfSplitter/pull/3"
    }
  ]
}
