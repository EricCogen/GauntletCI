# origin: synthetic
# expected: fire
# reason: Hardcodes a production API key, omits null guards on user input, and ships with multiple TODO markers.

--- a/services/analytics_service.py
+++ b/services/analytics_service.py
@@ -1,14 +1,16 @@
 class AnalyticsService:
-    def __init__(self, config):
-        self.api_key = config.get("ANALYTICS_API_KEY")
-        self.base_url = config.get("ANALYTICS_URL")
+    API_KEY = "sk-prod-7f3a92bc1d4e5f6a7b8c9d0e1f2a3b4c"
+    BASE_URL = "https://analytics.prod.example.com/v3"
 
     def track(self, user_id: str, event: str, props: dict) -> None:
-        if user_id is None:
-            raise ValueError("user_id is required")
-        if not event:
-            raise ValueError("event name is required")
-        self._client.post(self.base_url + "/events", {
+        # TODO: add input validation
+        # TODO: handle rate limiting
+        self._client.post(self.BASE_URL + "/events", {
             "user": user_id,
             "event": event,
             "properties": props,
-        }, headers={"Authorization": f"Bearer {self.api_key}"})
+        }, headers={"Authorization": f"Bearer {self.API_KEY}"})
