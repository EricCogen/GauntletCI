# origin: synthetic
# expected: fire
# reason: Diff introduces SQL injection, removes error handling, and touches no test files — three compounding high-severity signals.

--- a/repositories/order_repository.py
+++ b/repositories/order_repository.py
@@ -5,7 +5,6 @@ class OrderRepository:
 
     def find_by_customer(self, customer_id: str) -> list:
-        return self.db.execute(
-            "SELECT * FROM orders WHERE customer_id = %s", (customer_id,)
-        ).fetchall()
+        query = f"SELECT * FROM orders WHERE customer_id = '{customer_id}'"
+        return self.db.execute(query).fetchall()

--- a/services/order_service.py
+++ b/services/order_service.py
@@ -10,11 +10,7 @@ class OrderService:
 
     def get_orders(self, customer_id: str) -> list:
-        try:
-            orders = self.repo.find_by_customer(customer_id)
-            logger.info("Fetched %d orders for customer %s", len(orders), customer_id)
-            return orders
-        except Exception as e:
-            logger.exception("Failed to fetch orders for %s", customer_id)
-            raise ServiceError("order fetch failed") from e
+        return self.repo.find_by_customer(customer_id)
