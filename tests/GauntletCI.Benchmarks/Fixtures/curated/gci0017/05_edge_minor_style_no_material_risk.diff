# expected: do-not-fire
# reason: One minor style concern (variable name 'x') but no material risk indicators — does not meet the production readiness bar for GCI017.

--- a/utils/tax_calculator.py
+++ b/utils/tax_calculator.py
@@ -3,5 +3,5 @@ def calculate_tax(amount: float, rate: float) -> float:
     """Return the tax amount for the given amount and rate (0.0–1.0)."""
-    result = amount * rate
-    return round(result, 2)
+    x = amount * rate
+    return round(x, 2)
