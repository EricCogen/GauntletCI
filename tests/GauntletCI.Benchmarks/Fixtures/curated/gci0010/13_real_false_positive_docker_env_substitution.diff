# origin: real
# source: https://github.com/TimKenobi/StepScribe/pull/2
# expected: do-not-fire
# reason: Docker Compose uses ${VAR:-default} substitution so credentials come from the host environment at runtime.

--- a/docker-compose.yml
+++ b/docker-compose.yml
@@ -3,9 +3,9 @@ services:
     image: postgres:17-alpine
     container_name: stepscribe-db
     environment:
-      POSTGRES_USER: stepscribe
-      POSTGRES_PASSWORD: stepscribe
-      POSTGRES_DB: stepscribe
+      POSTGRES_USER: ${POSTGRES_USER:-stepscribe}
+      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-stepscribe}
+      POSTGRES_DB: ${POSTGRES_DB:-stepscribe}
     ports:
       - "${DB_PORT:-5432}:5432"
     healthcheck:
-      test: ["CMD-SHELL", "pg_isready -U stepscribe"]
+      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-stepscribe}"]
