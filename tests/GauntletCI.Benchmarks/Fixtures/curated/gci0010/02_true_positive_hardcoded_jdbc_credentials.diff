# expected: fire
# reason: Hardcodes a JDBC connection string with username and password directly in source code.

--- a/src/main/java/com/example/DatabaseConfig.java
+++ b/src/main/java/com/example/DatabaseConfig.java
@@ -8,8 +8,8 @@ public class DatabaseConfig {
 
     @Bean
     public DataSource dataSource() {
-        return DataSourceBuilder.create()
-            .url(env.getProperty("spring.datasource.url"))
-            .username(env.getProperty("spring.datasource.username"))
-            .password(env.getProperty("spring.datasource.password"))
-            .build();
+        HikariConfig cfg = new HikariConfig();
+        cfg.setJdbcUrl("jdbc:postgresql://prod-db.internal:5432/appdb");
+        cfg.setUsername("app_user");
+        cfg.setPassword("S3cur3P@ss!");
+        return new HikariDataSource(cfg);
     }
 }
