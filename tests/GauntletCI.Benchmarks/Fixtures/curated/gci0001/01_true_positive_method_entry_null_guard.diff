# expected: fire
# reason: New method-entry null guard introduces throw-based early exit with no visible validation tests.

--- a/src/UserService.cs
+++ b/src/UserService.cs
@@ -10,4 +10,9 @@ public class UserService
     public void Create(User user)
     {
+        if (user == null)
+            throw new ArgumentNullException(nameof(user));
+
         Save(user);
     }
