# expected: do-not-fire
# reason: Diff contains distant edits and no tight method-entry condition-to-exit linkage.

--- a/src/InventoryService.cs
+++ b/src/InventoryService.cs
@@ -8,6 +8,8 @@ public class InventoryService
     public void Update(Item item)
     {
+        Log(item.Id);
         EnsureConnected();
@@ -24,4 +26,6 @@ public class InventoryService
         cache[item.Id] = item;
+        Publish(item);
     }
