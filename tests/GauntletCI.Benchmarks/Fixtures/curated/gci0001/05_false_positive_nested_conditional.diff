# expected: do-not-fire
# reason: Nested conditional inside existing branch is not a method-entry guard.

--- a/src/NotificationService.cs
+++ b/src/NotificationService.cs
@@ -12,6 +12,11 @@ public class NotificationService
     public void Notify(User user)
     {
         if (user != null)
         {
+            if (user.Email == null)
+                return;
             Send(user);
         }
     }
