# expected: fire
# reason: Guard is present; rule should detect behavior change while confidence handling is managed by test assessment context.

--- a/src/AccountService.cs
+++ b/src/AccountService.cs
@@ -10,4 +10,7 @@ public class AccountService
     public void Open(Account account)
     {
+        if (account == null)
+            throw new ArgumentNullException(nameof(account));
         Save(account);
     }
--- a/tests/AccountServiceTests.cs
+++ b/tests/AccountServiceTests.cs
@@ -4,3 +4,4 @@ public class AccountServiceTests
+    [Fact] public void Open_Throws_WhenAccountMissing() { }
