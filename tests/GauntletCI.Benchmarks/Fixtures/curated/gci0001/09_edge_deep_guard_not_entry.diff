# expected: do-not-fire
# reason: Added guard is deep inside method logic and not near method entry.

--- a/src/WorkflowService.cs
+++ b/src/WorkflowService.cs
@@ -10,7 +10,12 @@ public class WorkflowService
     public void Run(Context ctx)
     {
         Prepare(ctx);
         Execute(ctx);
+        if (ctx.IsTransient)
+        {
+            if (ctx.Token == null)
+                return;
+        }
         Finalize(ctx);
     }
