# expected: fire
# reason: The payment route is registered in the router but the handler body is a TODO stub with no real implementation.

--- a/src/Routing/AppRouter.cs
+++ b/src/Routing/AppRouter.cs
@@ -12,6 +12,7 @@ public static class AppRouter
     public static void RegisterRoutes(IEndpointRouteBuilder app)
     {
         app.MapGet("/orders", OrderHandlers.GetAll);
+        app.MapPost("/payments", PaymentHandlers.ProcessPayment);
     }
 }

--- a/src/Handlers/PaymentHandlers.cs
+++ b/src/Handlers/PaymentHandlers.cs
@@ -0,0 +1,10 @@
+public static class PaymentHandlers
+{
+    public static IResult ProcessPayment(PaymentRequest request)
+    {
+        // TODO: implement payment processing via Stripe
+        return Results.Ok();
+    }
+}
