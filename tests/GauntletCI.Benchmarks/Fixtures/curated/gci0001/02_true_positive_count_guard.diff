# expected: fire
# reason: Added entry guard with return changes accepted input domain for empty collection case.

--- a/src/BatchProcessor.cs
+++ b/src/BatchProcessor.cs
@@ -20,5 +20,9 @@ public class BatchProcessor
     public void Run(List<Item> items)
     {
+        if (items.Count == 0)
+            return;
+
         Process(items);
     }
