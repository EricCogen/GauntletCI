# expected: do-not-fire
# reason: Guard condition already existed; replacement should not count as new validation boundary.

--- a/src/UserService.cs
+++ b/src/UserService.cs
@@ -8,6 +8,6 @@ public class UserService
-        if (user == null)
-            return;
+        if (user == null)
+            throw new ArgumentNullException(nameof(user));
         Save(user);
