# origin: real
# source: https://github.com/Shooshpanius/Pullenti.Core/pull/4
# expected: do-not-fire
# reason: Correctly replaces .Result blocking with async/await — no concurrency hazard introduced.

--- a/PullentiAPI/Controllers/NerController.cs
+++ b/PullentiAPI/Controllers/NerController.cs
@@ -23,10 +23,9 @@ public class Ner() : ControllerBase
     [HttpPost("getNer")]
     [Authorize(AuthenticationSchemes = "BasicAuthentication")]
 
-    public string getNerPOST([FromBody] RequestData requestData)
+    public async Task<string> getNerPOST([FromBody] RequestData requestData)
     {
-        Task<string> nerAsync = getNerAsync(requestData.text);
-        return nerAsync.Result;
+        return await getNerAsync(requestData.text);
     }
 }
