# origin: real
# source: https://github.com/steq98ai-githng/GY-langchain/pull/22
# expected: fire
# reason: Calls blocking requests.get() inside an async method, stalling the event loop for the full HTTP round-trip duration.

--- a/libs/langchain/langchain/memory/motorhead_memory.py
+++ b/libs/langchain/langchain/memory/motorhead_memory.py
@@ -44,12 +44,11 @@ def __get_headers(self) -> Dict[str, str]:
         return headers
 
     async def init(self) -> None:
-        async with aiohttp.ClientSession() as session:
-            async with session.get(
-                f"{self.url}/sessions/{self.session_id}/memory",
-                timeout=aiohttp.ClientTimeout(total=self.timeout),
-                headers=self.__get_headers(),
-            ) as res:
-                res_data = await res.json(content_type=None)
+        res = requests.get(
+            f"{self.url}/sessions/{self.session_id}/memory",
+            timeout=self.timeout,
+            headers=self.__get_headers(),
+        )
+        res_data = res.json()
         res_data = res_data.get("data", res_data)
