# origin: real
# source: https://github.com/hsahovic/poke-env/pull/858
# expected: fire
# reason: Locking around per-battle message handling is removed and lock cleanup is deleted, allowing concurrent interleaving on shared battle state with lingering lock bookkeeping drift.

--- a/src/poke_env/ps_client/ps_client.py
+++ b/src/poke_env/ps_client/ps_client.py
@@ -69,7 +69,6 @@ def __init__(
         self._active_tasks: Set[asyncio.Task] = set()
-        self._battle_locks: dict[str, asyncio.Lock] = {}
         self._open_timeout = open_timeout
         self._ping_interval = ping_interval
         self._ping_timeout = ping_timeout
@@ -139,9 +138,5 @@ async def _handle_message(self, message: str):
             if split_messages[0][0].startswith(">battle"):
                 battle_tag = split_messages[0][0][1:]
-                if battle_tag not in self._battle_locks:
-                    self._battle_locks[battle_tag] = asyncio.Lock()
-                async with self._battle_locks[battle_tag]:
-                    await self._handle_battle_message(split_messages)
+                await self._handle_battle_message(split_messages)

--- a/src/poke_env/player/player.py
+++ b/src/poke_env/player/player.py
@@ -325,7 +325,6 @@ async def _handle_battle_message(self, split_messages: List[List[str]]):
                 self._battle_finished_callback(battle)
                 async with self._battle_end_condition:
                     self._battle_end_condition.notify_all()
-                self.ps_client._battle_locks.pop(battle.battle_tag, None)
                 if hasattr(self.ps_client, "websocket"):
                     await self.ps_client.send_message(f"/leave {battle.battle_tag}")
