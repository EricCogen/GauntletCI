# origin: real
# source: https://github.com/abpframework/abp/pull/25155
# expected: fire
# reason: GetAwaiter().GetResult() blocks the calling thread, but the overall fix reads stdout and stderr concurrently via Task.WhenAll — debatable: avoids the pipe deadlock but still blocks the outer thread.

--- a/framework/src/Volo.Abp.Cli.Core/Volo/Abp/Cli/Utils/CmdHelper.cs
+++ b/framework/src/Volo.Abp.Cli.Core/Volo/Abp/Cli/Utils/CmdHelper.cs
@@ -137,14 +137,11 @@ public string RunCmdAndGetOutput(string command, out int exitCode, string workin
 
             process.Start();
 
-            using (var standardOutput = process.StandardOutput)
-            {
-                using (var standardError = process.StandardError)
-                {
-                    output = standardOutput.ReadToEnd();
-                    output += standardError.ReadToEnd();
-                }
-            }
+            var outputTask = process.StandardOutput.ReadToEndAsync();
+            var errorTask = process.StandardError.ReadToEndAsync();
+
+            var results = Task.WhenAll(outputTask, errorTask).GetAwaiter().GetResult();
+            output = results[0] + results[1];
 
             process.WaitForExit();
