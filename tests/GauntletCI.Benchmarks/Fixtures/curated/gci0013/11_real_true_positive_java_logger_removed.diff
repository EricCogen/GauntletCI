# origin: real
# source: https://github.com/elastic/elasticsearch/pull/120000
# expected: fire
# reason: Structured error log with document ID is removed from the catch block, silently discarding indexing failure details.

--- a/server/src/main/java/org/elasticsearch/index/engine/InternalEngine.java
+++ b/server/src/main/java/org/elasticsearch/index/engine/InternalEngine.java
@@ -743,7 +743,6 @@ public class InternalEngine extends Engine {
             final String documentId = index.id();
             final long seqNo = index.seqNo();
             try {
                 final IndexResult result = indexIntoLucene(index, plan);
                 return result;
             } catch (final Exception e) {
-                logger.error("Failed to index document [id={}] seqNo=[{}]", documentId, seqNo, e);
                 throw new EngineException(shardId, "Index failed for id=[" + documentId + "]", e);
             }
         }
@@ -768,9 +767,6 @@ public class InternalEngine extends Engine {
         try {
             return deleteIntoLucene(delete, plan);
         } catch (final IOException e) {
-            logger.error(
-                "Failed to delete document [id={}] from shard [{}]", delete.id(), shardId, e
-            );
             maybeFailEngine("delete", e);
             throw new EngineException(shardId, "Delete failed", e);
         }
