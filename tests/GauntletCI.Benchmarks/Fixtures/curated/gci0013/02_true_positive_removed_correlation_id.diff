# origin: synthetic
# expected: fire
# reason: Removes the correlationId field from the structured log entry on the payment flow, breaking distributed trace correlation.

--- a/src/middleware/paymentLogger.ts
+++ b/src/middleware/paymentLogger.ts
@@ -8,10 +8,8 @@ export function logPaymentAttempt(req: PaymentRequest, result: PaymentResult): void {
     logger.info("payment_attempt", {
         orderId: req.orderId,
-        correlationId: req.headers["x-correlation-id"],
         amount: req.amount,
         currency: req.currency,
         status: result.status,
         durationMs: result.durationMs,
     });
 }
