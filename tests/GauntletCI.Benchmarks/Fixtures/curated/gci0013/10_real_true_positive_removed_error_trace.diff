# origin: real
# source: https://github.com/open-telemetry/opentelemetry-collector/pull/12000
# expected: fire
# reason: Error log with trace_id correlation is removed from the span processing error handler, eliminating observability into pipeline failures.

--- a/receiver/otlpreceiver/internal/trace/otlp.go
+++ b/receiver/otlpreceiver/internal/trace/otlp.go
@@ -61,7 +61,6 @@ func (r *Receiver) Export(ctx context.Context, req *ptraceotlp.ExportRequest) (*
 
 	if err := r.nextConsumer.ConsumeTraces(ctx, td); err != nil {
 		status := consumererror.ToStatusError(err, codes.Unavailable)
-		level.Error(r.logger).Log("msg", "failed to process spans", "trace_id", span.TraceID().String(), "err", err)
 		return &ptraceotlp.ExportResponse{}, status
 	}
 
@@ -82,11 +81,7 @@ func (r *Receiver) processSpan(ctx context.Context, span ptrace.Span) error {
 		return nil
 	}
 	if err := r.validator.ValidateSpan(span); err != nil {
-		level.Error(r.logger).Log(
-			"msg", "span validation failed",
-			"trace_id", span.TraceID().String(),
-			"span_id", span.SpanID().String(),
-			"err", err,
-		)
 		return err
 	}
 	return nil
