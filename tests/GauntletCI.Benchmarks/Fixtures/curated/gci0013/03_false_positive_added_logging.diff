# origin: synthetic
# expected: do-not-fire
# reason: Adds additional console.log/logger calls to an existing function — observability is improved, not degraded.

--- a/src/services/checkoutService.ts
+++ b/src/services/checkoutService.ts
@@ -8,8 +8,12 @@ export class CheckoutService {
 
     async checkout(cartId: string): Promise<Order> {
+        logger.debug("checkout started", { cartId });
         const cart = await this.cartRepo.getById(cartId);
+        logger.debug("cart loaded", { cartId, itemCount: cart.items.length });
         const order = await this.orderService.create(cart);
+        logger.info("order created", { cartId, orderId: order.id });
         return order;
     }
 }
