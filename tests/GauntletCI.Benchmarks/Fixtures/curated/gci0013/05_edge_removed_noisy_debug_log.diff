# origin: synthetic
# expected: do-not-fire
# reason: Removes a noisy debug-level log, but all error and warn level logs remain intact — observability is not degraded.

--- a/src/services/sessionService.ts
+++ b/src/services/sessionService.ts
@@ -10,7 +10,6 @@ export class SessionService {
 
     getSession(token: string): Session | null {
-        logger.debug("session lookup", { token });
         const session = this.store.get(token);
         if (!session) {
             logger.warn("session not found", { token });
             return null;
         }
         return session;
     }
 }
