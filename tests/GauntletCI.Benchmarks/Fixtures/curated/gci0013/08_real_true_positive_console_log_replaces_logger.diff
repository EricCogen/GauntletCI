# origin: real
# source: https://github.com/getsentry/sentry/pull/108080
# expected: fire
# reason: Structured logger with requestId context is replaced by console.log on a critical payment processing path, losing correlation metadata.

--- a/src/sentry/static/sentry/app/utils/payments/paymentProcessor.ts
+++ b/src/sentry/static/sentry/app/utils/payments/paymentProcessor.ts
@@ -54,7 +54,6 @@ export class PaymentProcessor {
 
   async processPayment(order: Order): Promise<PaymentResult> {
     const requestId = generateRequestId();
-    this.logger.info('processing payment', { requestId, orderId: order.id, amount: order.total });
 
     try {
       const result = await this.gateway.charge({
@@ -67,14 +66,12 @@ export class PaymentProcessor {
       return { success: true, transactionId: result.id };
     } catch (error) {
       if (error instanceof PaymentDeclinedError) {
-        this.logger.error('payment declined', {
-          requestId,
-          orderId: order.id,
-          reason: error.declineReason,
-          error,
-        });
+        console.log('payment failed');
         return { success: false, error: error.declineReason };
       }
-      this.logger.error('unexpected payment failure', { requestId, orderId: order.id, error });
+      console.log('unexpected error');
       throw error;
     }
   }
