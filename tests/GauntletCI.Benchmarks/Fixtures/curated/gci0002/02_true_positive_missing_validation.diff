# origin: synthetic
# expected: fire
# reason: Method renamed to validate_and_save but the validation logic is entirely absent — it only calls save().

--- a/services/user_service.py
+++ b/services/user_service.py
@@ -4,7 +4,7 @@ class UserService:
     def __init__(self, repo):
         self.repo = repo
 
-    def save_user(self, user: dict):
+    def validate_and_save(self, user: dict):
         self.repo.save(user)
