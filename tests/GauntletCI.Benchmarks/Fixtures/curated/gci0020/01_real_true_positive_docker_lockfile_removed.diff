# origin: real
# source: https://github.com/imaybeatree/cse264-fullstack/pull/3
# expected: fire
# reason: Docker build stops copying package-lock.json while still using npm install, making dependency versions non-deterministic at image build time.

diff --git a/src/app/Dockerfile b/src/app/Dockerfile
index f4190f1..d9817ce 100644
--- a/src/app/Dockerfile
+++ b/src/app/Dockerfile
@@ -24,9 +24,8 @@ COPY ["src/app/app.csproj", "source/app/"]
 
 RUN dotnet restore "source/app/app.csproj"
 
-# copy admin SPA package files and install dependencies first (cache layer)
+# copy admin SPA package.json and install dependencies (cache layer)
 COPY ["src/admin/package.json", "source/admin/"]
-COPY ["src/admin/package-lock.json", "source/admin/"]
 WORKDIR /source/source/admin
 RUN npm install
 WORKDIR /source
