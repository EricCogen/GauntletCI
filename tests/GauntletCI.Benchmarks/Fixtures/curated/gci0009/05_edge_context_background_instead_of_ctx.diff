# origin: synthetic
# expected: fire
# reason: Uses db.QueryContext but passes context.Background() instead of propagating the caller's ctx, breaking trace propagation.

--- a/inventory/repository.go
+++ b/inventory/repository.go
@@ -8,7 +8,7 @@ func (r *InventoryRepo) CheckStock(ctx context.Context, sku string) (int, error) {
-    row := r.db.QueryRowContext(ctx, "SELECT qty FROM inventory WHERE sku = $1", sku)
+    row := r.db.QueryRowContext(context.Background(), "SELECT qty FROM inventory WHERE sku = $1", sku)
     var qty int
     if err := row.Scan(&qty); err != nil {
         return 0, err
     }
     return qty, nil
 }
