# origin: synthetic
# expected: fire
# reason: New async handler does not accept ctx context.Context, breaking the repo-wide convention for all HTTP handlers.

--- a/handlers/product_handler.go
+++ b/handlers/product_handler.go
@@ -5,9 +5,9 @@ import "net/http"
 
-func GetProductHandler(ctx context.Context, w http.ResponseWriter, r *http.Request) {
-    products, err := productRepo.FindAll(ctx)
+func GetProductHandler(w http.ResponseWriter, r *http.Request) {
+    products, err := productRepo.FindAll(context.Background())
     if err != nil {
         http.Error(w, err.Error(), http.StatusInternalServerError)
         return
     }
     json.NewEncoder(w).Encode(products)
 }
