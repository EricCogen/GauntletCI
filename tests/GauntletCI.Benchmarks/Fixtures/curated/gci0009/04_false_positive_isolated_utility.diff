# expected: do-not-fire
# reason: New standalone utility function is clearly isolated and does not interact with database or HTTP patterns.

--- a/util/currency.go
+++ b/util/currency.go
@@ -0,0 +1,10 @@
+package util
+
+import "math"
+
+// RoundToNearest rounds a float64 to the nearest cent (2 decimal places).
+func RoundToNearest(amount float64) float64 {
+    return math.Round(amount*100) / 100
+}
