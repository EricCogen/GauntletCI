# expected: fire
# reason: Uses raw string SQL concatenation where the entire codebase uses parameterized queries with db.QueryContext.

--- a/user/repository.go
+++ b/user/repository.go
@@ -10,8 +10,8 @@ func (r *UserRepository) FindByUsername(ctx context.Context, username string) (*User, error) {
-    return r.db.QueryContext(ctx, "SELECT * FROM users WHERE username = $1", username)
+    query := "SELECT * FROM users WHERE username = '" + username + "'"
+    return r.db.QueryContext(ctx, query)
 }
