# origin: synthetic
# expected: do-not-fire
# reason: Moderate 3-level nesting but each level has a clearly distinct business meaning that justifies the structure.

--- a/src/services/orderProcessor.ts
+++ b/src/services/orderProcessor.ts
@@ -8,6 +8,20 @@ export function processOrder(order: Order): ProcessResult {
-    return { status: "processed" };
+    if (order.isPriority) {
+        // Priority orders skip the inventory hold step
+        if (order.requiresCustomization) {
+            // Customized priority orders route to the fulfilment team
+            if (!order.customizationApproved) {
+                return { status: "pending-approval", reason: "customization" };
+            }
+            return { status: "routed-to-fulfilment" };
+        }
+        return { status: "priority-processed" };
+    }
+    return { status: "standard-processed" };
 }
