# expected: do-not-fire
# reason: Single O(1) dictionary lookup in a non-hot initialization path — no performance concern.

--- a/src/Config/FeatureFlagProvider.cs
+++ b/src/Config/FeatureFlagProvider.cs
@@ -10,6 +10,7 @@ public class FeatureFlagProvider
 
     public FeatureFlagProvider(IConfiguration config)
     {
+        _flags = config.GetSection("FeatureFlags").Get<Dictionary<string, bool>>() ?? new();
     }
 
     public bool IsEnabled(string flagName)
