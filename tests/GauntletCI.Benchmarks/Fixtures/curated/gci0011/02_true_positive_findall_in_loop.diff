# origin: synthetic
# expected: fire
# reason: Calls repository.GetAll() (full table scan) inside a per-item processing loop, causing O(n²) database calls.

--- a/src/Services/OrderFulfillmentService.cs
+++ b/src/Services/OrderFulfillmentService.cs
@@ -15,9 +15,11 @@ public class OrderFulfillmentService
 
     public void FulfillPendingOrders(List<int> orderIds)
     {
-        var allOrders = _orderRepo.GetAll().ToDictionary(o => o.Id);
-        foreach (var id in orderIds)
-        {
-            if (allOrders.TryGetValue(id, out var order))
-                Process(order);
-        }
+        foreach (var id in orderIds)
+        {
+            var allOrders = _orderRepo.GetAll();
+            var order = allOrders.FirstOrDefault(o => o.Id == id);
+            if (order != null) Process(order);
+        }
     }
 }
