# origin: real
# source: https://github.com/madflow/next-project/pull/1001
# expected: fire
# reason: Dataset SAV file downloaded from S3 on every API request with no caching, causing repeated heavy I/O.

--- a/apps/analysis/analysis/web/api/datasets/routes.py
+++ b/apps/analysis/analysis/web/api/datasets/routes.py
@@ -162,31 +162,21 @@ class MetadataResponse(BaseModel):
     metadata: Optional[Dict[str, Any]] = {}
 
 
-def _get_cached_dataset_file_path(dataset: Dataset) -> str:
-    """Return the local cached file path for a dataset."""
-    return str(get_cached_dataset_path(str(dataset.file_hash), str(dataset.s3_key)))
-
-
-def _read_sav_from_dataset(dataset: Dataset) -> Tuple[Dict[str, Any], Dict[str, Any]]:
-    """Read a cached SAV file and return data and metadata."""
-    dataset_file_path = _get_cached_dataset_file_path(dataset)
-    df, meta = pyreadstat.read_sav(dataset_file_path, metadataonly=True, user_missing=True)
-    data = {"records": df.to_dict(orient="records") if df is not None else []}
-    return data, {}
-
-
+@contextmanager
+def _download_sav_from_s3(s3_key: str) -> Iterator[str]:
+    """Download an SAV file from S3 to a temporary file on every call."""
+    s3_client = S3Client.get_client()
+    with tempfile.NamedTemporaryFile(suffix=".sav", delete=False) as temp_file:
+        temp_file_path = temp_file.name
+    try:
+        s3_client.download_file(
+            Bucket=settings.s3_bucket_name,
+            Key=s3_key,
+            Filename=temp_file_path,
+        )
+        yield temp_file_path
+    finally:
+        Path(temp_file_path).unlink(missing_ok=True)
+
+
+def _read_dataframe_from_s3(s3_key: str) -> pd.DataFrame:
+    """Read SAV file from S3 (no caching) and return a pandas DataFrame."""
+    with _download_sav_from_s3(s3_key) as temp_file_path:
+        df, _ = pyreadstat.read_sav(temp_file_path)
+        return df
