# expected: fire
# reason: Uses .Count() > 0 instead of .Any() inside a hot loop, causing unnecessary full enumeration of each collection every iteration.

--- a/src/Services/ReportService.cs
+++ b/src/Services/ReportService.cs
@@ -18,9 +18,9 @@ public class ReportService
 
     public List<ReportLine> BuildReport(List<Order> orders)
     {
         return orders.Select(o => new ReportLine
         {
             OrderId = o.Id,
-            HasItems = o.Items.Any(),
+            HasItems = o.Items.Count() > 0,
             TotalValue = o.Items.Sum(i => i.Price)
         }).ToList();
     }
 }
