# origin: real
# source: https://github.com/django/django/pull/20587
# expected: do-not-fire
# reason: The old API remains as a compatibility shim and forwards to the new method with a warning, reducing immediate breakage risk.

--- a/django/db/models/sql/compiler.py
+++ b/django/db/models/sql/compiler.py
@@ -547,6 +547,19 @@ def quote_name(self, name):
         self.quote_cache[name] = quoted
         return quoted
 
+    # RemovedInDjango70Warning: When the deprecation ends, remove.
+    def quote_name_unless_alias(self, name):
+        warnings.warn(
+            (
+                "SQLCompiler.quote_name_unless_alias() is deprecated. "
+                "Use .quote_name() instead."
+            ),
+            category=RemovedInDjango70Warning,
+            skip_file_prefixes=django_file_prefixes(),
+        )
+        return self.quote_name(name)
+
     def compile(self, node):
         vendor_impl = getattr(node, "as_" + self.connection.vendor, None)
