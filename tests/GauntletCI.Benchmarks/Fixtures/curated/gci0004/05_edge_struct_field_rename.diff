# expected: fire
# reason: Renaming the exported struct field 'ID' to 'UserId' silently breaks JSON marshaling contracts for all downstream API consumers.

--- a/user/model.go
+++ b/user/model.go
@@ -3,7 +3,7 @@ package user
 
 type User struct {
-    ID    int    `json:"id"`
+    UserId int   `json:"user_id"`
     Name  string `json:"name"`
     Email string `json:"email"`
 }
