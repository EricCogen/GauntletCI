# expected: fire
# reason: Removes the required 'timeout' parameter from the exported function GetUser, breaking all existing callers.

--- a/user/client.go
+++ b/user/client.go
@@ -5,7 +5,7 @@ import "context"
 
-func GetUser(ctx context.Context, id int, timeout time.Duration) (*User, error) {
+func GetUser(ctx context.Context, id int) (*User, error) {
-    ctx, cancel := context.WithTimeout(ctx, timeout)
-    defer cancel()
     return fetchUserFromDB(ctx, id)
 }
