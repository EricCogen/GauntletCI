# expected: do-not-fire
# reason: Adds a brand-new exported function DeleteUser without modifying any existing function signatures or types.

--- a/user/service.go
+++ b/user/service.go
@@ -10,3 +10,10 @@ func GetUser(id int) (*User, error) {
     return db.QueryUser(id)
 }
+
+// DeleteUser removes a user record by ID. Returns an error if not found.
+func DeleteUser(ctx context.Context, id int) error {
+    if _, err := db.QueryUser(id); err != nil {
+        return fmt.Errorf("user %d not found: %w", id, err)
+    }
+    return db.DeleteUser(ctx, id)
+}
