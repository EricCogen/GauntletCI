# origin: synthetic
# expected: fire
# reason: Modifying the elif/else routing causes error code 422 to fall through the branch unhandled, silently swallowed.

--- a/src/middleware/errorHandler.ts
+++ b/src/middleware/errorHandler.ts
@@ -5,11 +5,9 @@ export function handleApiError(code: number, res: Response): void {
     if (code === 400) {
         res.status(400).json({ error: "Bad Request" });
-    } else if (code === 422) {
-        res.status(422).json({ error: "Unprocessable Entity" });
     } else if (code === 500) {
         res.status(500).json({ error: "Internal Server Error" });
     } else {
-        res.status(code).json({ error: "Unknown Error" });
+        // removed generic fallback — callers should only pass known codes
     }
 }
