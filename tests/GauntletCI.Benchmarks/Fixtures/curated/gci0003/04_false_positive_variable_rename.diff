# expected: do-not-fire
# reason: Internal variable renamed from 'tmp' to 'result' with no change to logic — all observable behavior is identical.

--- a/src/utils/slugify.ts
+++ b/src/utils/slugify.ts
@@ -2,7 +2,7 @@ export function slugify(text: string): string {
-    const tmp = text.toLowerCase().trim().replace(/\s+/g, "-");
-    return tmp.replace(/[^\w-]/g, "");
+    const result = text.toLowerCase().trim().replace(/\s+/g, "-");
+    return result.replace(/[^\w-]/g, "");
 }
