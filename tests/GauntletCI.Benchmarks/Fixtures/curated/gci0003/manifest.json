{
  "mapped_gci_rules": [
    "GCI003"
  ],
  "description": "Behavioral Change Detection: Detect material runtime behavior changes.",
  "fixtures": [
    {
      "id": "gci0003-10",
      "diff_file": "10_real_edge_blocking_wrapper_with_concurrent_stream_reads.diff",
      "category": "edge-case",
      "expected_outcome": "edge-case",
      "expected_gci_rules": [
        "GCI003"
      ],
      "notes": "Process stream handling is improved to read stdout/stderr concurrently, but outer call still synchronously blocks on completion, making behavioral impact nuanced.",
      "origin": "real",
      "source_url": "https://github.com/abpframework/abp/pull/25155"
    }
  ]
}
