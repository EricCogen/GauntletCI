# expected: do-not-fire
# reason: Refactors a filter+map list comprehension to an equivalent for-loop — output is identical and no behavior changes.

--- a/src/utils/transform.ts
+++ b/src/utils/transform.ts
@@ -1,5 +1,10 @@
 export function getActiveNames(users: User[]): string[] {
-    return users.filter(u => u.active).map(u => u.name);
+    const names: string[] = [];
+    for (const u of users) {
+        if (u.active) {
+            names.push(u.name);
+        }
+    }
+    return names;
 }
