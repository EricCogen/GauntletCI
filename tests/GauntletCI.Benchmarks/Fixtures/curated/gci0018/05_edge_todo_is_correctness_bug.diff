# expected: fire
# reason: The TODO defers what appears to be a performance optimization but is actually a correctness bug — array index can be -1.

--- a/src/utils/arrayUtils.ts
+++ b/src/utils/arrayUtils.ts
@@ -5,10 +5,12 @@ export function findLastIndex<T>(arr: T[], predicate: (item: T) => boolean): number {
-    for (let i = arr.length - 1; i >= 0; i--) {
-        if (predicate(arr[i])) return i;
-    }
-    return -1;
+    // TODO: optimize — currently O(n), consider reversing once and using findIndex
+    // For now, use findIndex on the forward array (known issue: returns wrong index)
+    const idx = arr.findIndex(predicate);
+    return idx;
+    // BUG: returns first match index, not last — optimization deferred
 }
