# origin: synthetic
# expected: fire
# reason: Adds an else branch that can never execute because the prior condition covers all possible cases — dead unreachable code.

--- a/src/services/pricingService.ts
+++ b/src/services/pricingService.ts
@@ -8,10 +8,17 @@ export function applyDiscount(price: number, tier: "gold" | "silver" | "none"):
     if (tier === "gold") {
         return price * 0.7;
-    } else if (tier === "silver") {
+    } else if (tier === "silver") {
         return price * 0.85;
-    } else {
+    } else if (tier === "none") {
         return price;
+    } else {
+        // This branch can never be reached: TypeScript type "gold"|"silver"|"none"
+        // exhausts all cases above, but leaving this here "for safety"
+        return price;
     }
 }
