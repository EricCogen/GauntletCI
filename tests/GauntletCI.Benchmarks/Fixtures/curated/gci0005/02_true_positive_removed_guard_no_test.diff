# origin: synthetic
# expected: fire
# reason: Removes the null-check guard from a public API method without updating any test to cover the null path removal.

--- a/src/main/java/com/example/UserApi.java
+++ b/src/main/java/com/example/UserApi.java
@@ -8,9 +8,6 @@ public class UserApi {
 
     public UserDto getUser(Integer userId) {
-        if (userId == null) {
-            throw new IllegalArgumentException("userId must not be null");
-        }
         return repository.findById(userId)
                 .map(UserMapper::toDto)
                 .orElseThrow(() -> new UserNotFoundException(userId));
     }
 }
