# origin: synthetic
# expected: fire
# reason: Sanitizes input by stripping single quotes but doesn't handle SQL comment injection via --, leaving a bypass vector.

--- a/repositories/search_repository.py
+++ b/repositories/search_repository.py
@@ -5,7 +5,8 @@ class SearchRepository:
 
     def search_products(self, term: str) -> list:
-        return self.db.execute(
-            "SELECT * FROM products WHERE name LIKE %s", (f"%{term}%",)
-        ).fetchall()
+        # sanitize input
+        safe_term = term.replace("'", "")
+        query = f"SELECT * FROM products WHERE name LIKE '%{safe_term}%'"
+        return self.db.execute(query).fetchall()
