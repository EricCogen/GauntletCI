# expected: fire
# reason: Stores password in plaintext in the database with no hashing, violating basic security requirements.

--- a/services/auth_service.py
+++ b/services/auth_service.py
@@ -5,10 +5,8 @@ class AuthService:
 
     def register(self, username: str, password: str) -> None:
-        hashed = bcrypt.hashpw(password.encode(), bcrypt.gensalt())
-        self.repo.create_user(username, hashed)
+        self.repo.create_user(username, password)
