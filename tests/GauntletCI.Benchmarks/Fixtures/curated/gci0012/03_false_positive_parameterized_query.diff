# origin: synthetic
# expected: do-not-fire
# reason: Uses psycopg2 parameterized query with %s placeholder — user input is safely separated from the SQL statement.

--- a/repositories/order_repository.py
+++ b/repositories/order_repository.py
@@ -8,6 +8,10 @@ class OrderRepository:
 
+    def find_by_status(self, status: str) -> list:
+        return self.conn.execute(
+            "SELECT * FROM orders WHERE status = %s",
+            (status,)
+        ).fetchall()
+
     def find_all(self) -> list:
         return self.conn.execute("SELECT * FROM orders").fetchall()
