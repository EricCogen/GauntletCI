# origin: real
# source: https://github.com/home-assistant/core/pull/160216
# expected: fire
# reason: SSL verification becomes user-configurable and can be disabled, weakening transport security guarantees for remote calendar ingestion.

--- a/homeassistant/components/remote_calendar/coordinator.py
+++ b/homeassistant/components/remote_calendar/coordinator.py
@@ -7,7 +7,7 @@
 from ical.calendar import Calendar
 
 from homeassistant.config_entries import ConfigEntry
-from homeassistant.const import CONF_URL
+from homeassistant.const import CONF_URL, CONF_VERIFY_SSL
 from homeassistant.core import HomeAssistant
 from homeassistant.helpers.httpx_client import get_async_client
 from homeassistant.helpers.update_coordinator import DataUpdateCoordinator, UpdateFailed
@@ -42,7 +42,9 @@ def __init__(
             config_entry=config_entry,
             always_update=True,
         )
-        self._client = get_async_client(hass)
+        self._client = get_async_client(
+            hass, verify_ssl=config_entry.data.get(CONF_VERIFY_SSL, True)
+        )
         self._url = config_entry.data[CONF_URL]
 
     async def _async_update_data(self) -> Calendar:
